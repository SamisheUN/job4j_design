package ru.job4j.io;

/**
 * 0 ознакомиться с предыдущими уроками и кодом
 * 0.1 прикинуть как из файла прочитать просто текст, а там уже его рубить по разделителям. просто потоки. см. упр.
 * https://job4j.ru/profile/exercise/45/task-view/310
 * <p>
 * 1. Ознакомиться с форматом CSV.
 * 2 сотворить ввод переменных путь к файлу path, разделитель delimiter, приемник данных out и фильтр по столбцам filter.
 * Ключ -out имеет только два допустимых значения: stdout или путь к файлу, куда нужно вывести.
 * 3
 */
public class CSVReader {
    public static void handle(ArgsName argsName) throws Exception {
/*
        String path = argsName.get(path);
        String delimiter = argsName.get(delimiter);
        String filter = argsName.get(filter);
        String out = argsName.get(out);
        Scanner scanner = new Scanner(path);
        scanner.useDelimiter(delimiter);
        while (scanner.hasNext()) {
            System.out.println("Строка: " + scanner.next());
        }
        scanner.close();*/
    }

    public static void main(String[] args) {
        // ArgsName argsName = ArgsName.of(args);
    }
}
